# MOVED — UAT is its own arc now

This directory briefly held an A6 **slice05 = UAT** (arc-plan v1.8). On seeing the first
feedback batch, the UAT / CLI-hardening work proved **arc-sized and model-level** (shared
styling-crate extraction; `odd`→`design` + new `research` type; a `list` overhaul; command
renames), so it was **extracted to its own arc** (arc-plan v1.9).

- **A6** is paused after slice04 and resumes at **slice05 (PM-skill)**.
- The UAT / CLI-hardening arc holds the findings-disposition ledger + the chunked cc-prompts.

*(This dir is a tombstone — it couldn't be deleted from the sandbox this session; safe to
remove.)*
