# Slice 02 (Store Lifecycle): `store status`

> Per LEDGER-DISCIPLINE v2.0 (slice scale, §A). Evidence strength `asserted < attested < reproduced <
> reconciled`; a `done` row reaches ≥ `reproduced`. Code/fixtures class-(a): CDC reproduces by direct read;
> runtime (`cargo`/`clippy`/a live `store status` run) attested→CI / operator. Five-iteration cap.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | **`store status` exists + reports the pending delta**: `odm store status` shows the node delta (created/modified/removed, by type) in odm terms — **the same `delta::compute` that `commit` uses** | fixture: mutate a node → `store status` → the delta shows (by type, matching `commit`'s summary format) | serious (the read half) | arc-plan SL-2 | done | `store_status.rs::dirty_store_shows_the_pending_delta` + `dirty_store_matches_commits_own_delta_computation` — the latter runs `status --json` and `commit --dry-run --json` on the identical dirty worktree and asserts the two `delta` objects are equal, not just individually plausible. `store_cmd.rs::status` calls `odm_store::delta::compute` — the exact function `commit` calls — with no local recomputation. | |
| F-2 | **Clean store → "nothing to commit"**: a clean worktree reports "nothing to commit" (exit 0, not an error, not silence) | run on a clean store → clear message, exit 0 | serious | slice-doc | done | `clean_store_reports_nothing_to_commit` (`--json`: `clean: true`) + `clean_store_reports_in_plain_text_too` (stderr contains "nothing to commit"). Driven by `Repo::is_clean()` (the same flag `commit` branches on), not `delta.is_empty()` — see Notes. | `delta_phrase` uses `clean`, not `delta.is_empty()`, so a dirty-but-node-delta-empty worktree (e.g. only `config.toml` touched) still reports its `commit`-equivalent `auto_message` fallback rather than falsely claiming "nothing to commit" — mirrors `commit`'s own clean/dirty split exactly. |
| F-3 | **Ahead/behind reported**: when the store branch has unpushed local commits → "N commits ahead"; when upstream has moved on → "upstream is ahead"; when synced → "up to date" | fixture or manual: local branch with unpushed commits → `store status` → "N commits ahead" | serious (the new dimension over commit) | arc-plan SL-2 | done | `local_ahead_of_upstream_reports_the_count` + `local_ahead_reports_in_plain_text_too` + `synced_store_reports_up_to_date` — real fixtures against a **bare-repo remote** (`with_fetched_upstream`: `git init --bare` + `remote add origin` + `push` + `fetch`, no network), not a mock. Confirms the exact ahead count (2) and `behind: 0` after two unpushed commits, and `up-to-date`/`ahead:0,behind:0` right after a push+fetch. Reuses `init.rs`'s `Ancestry`/`SyncAction`/`sync_action()` + `worktree::rev_parse`/`is_ancestor`/`count_commits` — but does **not** fetch (D-1). | Also computes `behind` (an independent `count_commits(local..upstream)`), which `Ancestry` alone doesn't carry — `init::sync()` only ever needed `local_ahead`, since its callers don't display a behind-count; `status`'s JSON contract needs both unconditionally, so `status` computes both directly rather than stretching `Ancestry` to hold a field none of its other callers need. |
| F-4 | **No upstream → graceful**: when no remote-tracking branch is configured, reports "no upstream configured" (not an error) | run on a store with no remote → clear message, exit 0 | serious | slice-doc | done | `no_upstream_is_not_an_error` (`upstream: null`, exit 0) + `no_upstream_reports_in_plain_text_too` (stderr contains "no upstream configured"). The `SyncAction::NoUpstream` arm — `upstream_ref.rev_parse` returning `None` short-circuits straight to it, no error path. | |
| F-5 | **`--json`**: `{clean, branch, delta, upstream: {ref, ahead, behind, action} \| null, store_root}` | `--json` parses + fields correct on dirty, clean, ahead, and no-upstream runs | serious (LLM ergonomics) | ODD-0023 | done | `json_shape_is_correct` + every other fixture above asserts individual fields via `--json`. Exact shape implemented: `StatusJson{clean, branch, store_root, delta, upstream: Option<UpstreamJson>}`, `UpstreamJson{ref, action, ahead, behind}` (`ref` via `#[serde(rename = "ref")]` on a `reference` field — `ref` is a Rust keyword). `action` values: `up-to-date` \| `local-ahead` \| `upstream-ahead` \| `diverged` (no `no-upstream` value — that state is `upstream: null` instead, per F-4). | |
| F-6 | **Read-only — no mutation**: the store worktree and orphan branch are **bit-identical** before and after `status` — no commit, no fetch, no index write, no worktree write | fixture: snapshot the store state (e.g. `HEAD` oid + worktree file hashes) before and after `store status` → identical | serious (the distinguishing constraint) | slice-doc | done | `status_never_mutates_the_store_or_the_orphan_branch` — dirties the worktree, sets up a real fetched upstream, snapshots `HEAD` oid + `git status --porcelain` + the store's node-path listing, runs `store status --json`, asserts all three identical after. Also asserts a second `status` run produces byte-identical JSON output — proof no hidden fetch silently changed the remote-tracking ref between calls. Enforced by construction too: `status` calls only `delta::compute`, `Repo::is_clean`, and the read-only `worktree::{rev_parse,is_ancestor,count_commits}` — never `commit_all`, `fetch`, or any write. | |
| F-7 | **No model drift**: CLI + `odm-store` plumbing only; no node-schema change; ODD-0022 amended only if a line is genuinely needed | cross-read: diff scope is CLI + store; ODD cited if touched | correctness | ODD-0022 | done | Diff: `odm-cli/src/lib.rs` (`StoreCommand::Status` variant + dispatch arm), `odm-cli/src/store_cmd.rs` (`status` + `StatusJson`/`UpstreamJson`/helpers), `odm-cli/tests/store_status.rs` (new). **No** `odm-store` source change — every function `status` calls (`delta::compute`, `Repo::is_clean`, `worktree::rev_parse`/`is_ancestor`/`count_commits`, `init::{Ancestry,SyncAction,sync_action,DEFAULT_REMOTE}`) already existed and was already `pub`, exactly as the cc-prompt's "if visibility needs widening" note anticipated but did not require. No ODD-0022 amendment — this is plumbing the model already implies. | |
| F-8 | **Clippy clean; no `unsafe`; new code covered** | clippy `-D warnings` exit 0; `! grep unsafe`; fixtures cover dirty/clean/ahead/no-upstream/json | polish | CLAUDE.md | done | `make lint` (clippy `-D warnings` + rustfmt --check) clean; `grep unsafe` on the new/changed files finds none; `make test` (full workspace incl. doctests) green — 12 new `store_status.rs` fixtures cover dirty/clean/ahead/synced/no-upstream/json/read-only/no-store-section, all passing on the first run. | |
