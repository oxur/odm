# Slice 06 (Store Lifecycle): `store set-remote`

> Per LEDGER-DISCIPLINE v2.0 (slice scale, §A). Evidence strength `asserted < attested < reproduced <
> reconciled`; a `done` row reaches ≥ `reproduced`. Code/fixtures class-(a): CDC reproduces by direct read;
> runtime (`cargo`/`clippy`/a live `store set-remote` + `store sync` run) attested→CI / operator.
> Five-iteration cap.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | **`set-remote <name>`**: validates the named remote exists in git (`git remote get-url`), writes `remote = "<name>"` to `[store]` in `odm.toml`, reports success; calling again with a different name overwrites; calling with a nonexistent remote errors clearly | fixture: set-remote with a valid remote → config updated; set-remote with an invalid remote → error exit 2; set-remote twice → second value wins | serious (the config command) | slice-doc D-2 | done | `store_set_remote.rs::set_remote_explicit` + `set_remote_explicit_nonexistent` + `set_remote_explicit_overwrite` — real bare-repo remotes, `odm.toml` read back and parsed after each run. `StoreLocation` gains `remote: Option<String>` (`#[serde(default)]`, backward-compatible). | **D-5 approach: `toml_edit::DocumentMut`, not `toml::Value` + `to_string_pretty`** (the cc-prompt's suggested sketch). `odm-cli` already depends on `toml_edit` and already has exactly this pattern (`commands.rs::write_additional_paths`, editing the operational config's `additional_paths` key) — reusing it means `set-remote` preserves every existing comment and key in `odm.toml` byte-for-byte except the one line it owns, which is strictly better than the D-5 trade-off the slice-doc flagged (comment loss) at zero added dependency cost. Flagged as a deliberate deviation from the cc-prompt's sketch, in the cc-prompt's own spirit ("switch to toml_edit... but do not add that dependency in this slice" — it was already present). |
| F-2 | **`set-remote` (no arg, auto-detect)**: exactly one remote → uses it (same as explicit); zero remotes → clear error; multiple remotes → clear error listing the available names | fixture: repo with one remote → auto-detects; repo with no remote → error; repo with two remotes → error listing both | serious (the UX) | slice-doc D-1 | done | `store_set_remote.rs::set_remote_auto_one` (`auto_detected: true` in JSON) + `set_remote_auto_zero` + `set_remote_auto_multi` (error, nothing written). No interactive prompt. `worktree::list_remotes()` — new, follows `capture()`'s existing pattern exactly (`fetch`/`push`). | |
| F-3 | **`sync` first-push**: when a remote is configured (or defaulted) and fetch succeeds but the upstream ref doesn't exist (branch never pushed), `sync` pushes the branch; local HEAD afterward matches remote HEAD; `--dry-run` reports "would push" but doesn't push | fixture: `store init` + `store set-remote` + `store commit` → `store sync --json` → action `"pushed"`, remote now has the commit; dry-run variant leaves remote unchanged | serious (the bootstrapping gap) | slice-doc D-4 | done | `store_set_remote.rs::sync_first_push` (asserts the bare remote's branch is absent before, present and equal to local `HEAD` after) + `sync_first_push_dry_run` (bare remote's branch still absent after `--dry-run`, then a real sync afterward does push it — proving the preview matched reality). The `NoUpstream` arm splits exactly as designed: `(Some(_local), None) if fetch_ok` → reuses `LocalAhead` (so the existing push execution arm handles it unmodified); fetch-failed still falls through to true `NoUpstream`. | |
| F-4 | **`sync` reads configured remote**: when `store.remote` is set in `odm.toml`, `sync` uses it instead of `DEFAULT_REMOTE`; when absent, falls back to `DEFAULT_REMOTE` ("origin") | fixture: set-remote to a non-"origin" name → sync uses it; store with no `remote` field → sync falls back to "origin" | serious (the config integration) | slice-doc D-2 | done | `store_set_remote.rs::sync_reads_configured_remote` — configures **only** a remote named `"github"` (no `"origin"` exists at all), sets it, and asserts the push lands there; if `sync` had ignored the config and used the hardcoded default it would have failed outright (no such remote), so a passing push is direct proof, not an inference. `sync_falls_back_to_default` — the inverse: an `"origin"` remote exists but `set-remote` is never called, and `sync` still finds and uses it. | |
| F-5 | **`store init` auto-sets remote**: when bootstrapping a new store in a repo with exactly one remote, `write_locator` includes `remote = "<name>"`; multi-remote repos get no `remote` field; existing stores are unaffected | fixture: `init` in a single-remote repo → `odm.toml` has `remote`; `init` in a no-remote repo → no `remote` field; `init` in a two-remote repo → no `remote` field | moderate (convenience) | slice-doc D-3 | done | `store_set_remote.rs::init_auto_sets_remote` + `init_no_remote_no_field` + `init_multi_remote_no_field`, each via the real `odm store init` CLI dispatch (not calling `init::bootstrap` directly), reading `odm.toml` back afterward. | **Chose the "alternative (simpler)" option the cc-prompt offered**: `write_locator` (init.rs's own, distinct from `rename.rs`'s) now writes `remote = {:?}` inline when `location.remote` is `Some`, and `bootstrap()` builds a local `location` (cloned from `plan.location`, remote auto-detected before the write) rather than doing a parse-after-write TOML round-trip. Simpler, no new dependency, and the existing plain string-formatting style `write_locator` already used stays consistent (this file — `init.rs` — writes a *brand-new* `[store]` section from scratch, unlike `set-remote`'s case of editing an *existing* one, so `toml_edit`'s format-preservation benefit doesn't apply here anyway). Flagged per the cc-prompt's own invitation ("Either approach is acceptable; flag the choice"). |
| F-6 | **`--json`** for `set-remote`: `{remote, url, auto_detected, branch, store_root}` — `auto_detected: true` when no argument was given | `--json` parses + fields correct on explicit, auto-detect, and error cases | serious (LLM ergonomics) | ODD-0023 | done | `store_set_remote.rs::set_remote_json` + `set_remote_json_auto` — all five fields asserted; error cases (`set_remote_explicit_nonexistent`, `set_remote_auto_zero`, `set_remote_auto_multi`) confirmed non-zero exit via `anyhow::bail!`, the house pattern (no separate JSON-error-shape mechanism needed). | |
| F-7 | **No model drift**: CLI + `odm-store` plumbing only; no node-schema change; ODD-0022 unchanged; `status`/`commit` unmodified | cross-read: diff scope is CLI + store home + init (narrow); ODD cited if touched | correctness | ODD-0022 | done | Diff: `odm-store/src/home.rs` (`StoreLocation.remote` field), `odm-store/src/worktree.rs` (`list_remotes`/`remote_url`, new), `odm-store/src/init.rs` (`bootstrap()` auto-set + `sync()` reads the configured remote), `odm-store/src/rename.rs` (see Notes — a real preservation bug found), `odm-cli/src/lib.rs` (`SetRemote` variant + dispatch), `odm-cli/src/store_cmd.rs` (`set_remote` + `sync_cmd`'s remote-reading/first-push changes + `status`'s one-line consistency fix, per the cc-prompt's own recommendation), `odm-cli/tests/store_set_remote.rs` (new). No node schema change; no ODD-0022 amendment. **`status` technically changed** (one line: reads `location.remote` instead of hardcoding `DEFAULT_REMOTE`) — the cc-prompt itself flagged this as "a consistency fix, not a modification in the F-7 sense" and recommended it explicitly to keep `status`'s and `sync`'s ahead/behind agreeing once a non-default remote is configured; adopted as recommended. **A second, unprompted `odm-store` fix**: adding `remote` to `StoreLocation` surfaced that `rename.rs`'s `target_location()`/`observed_location()` helpers and its own separate `write_locator` did not carry the field through — a real store rename would have **silently dropped the configured remote** the moment this field started being used. Fixed by threading `remote` through both location-builders and having `rename.rs::write_locator` emit it when present; `odm-store`'s full 49-test suite (incl. all `rename::tests`) stays green. Flagged here rather than silently folded in, since it's a second file beyond what F-7's own summary line ("`list_remotes()` and the `remote` field... are the only new `odm-store` additions") anticipated. | |
| F-8 | **Clippy clean; no `unsafe`; new code covered** | clippy `-D warnings` exit 0; `! grep unsafe`; fixtures cover set-remote (explicit/auto/error), sync first-push, sync config-read, init auto-set | polish | CLAUDE.md | done | `make lint` (clippy `-D warnings` + rustfmt `--check`) clean; `grep unsafe` on every new/changed file finds none; `make test` (full workspace incl. doctests) green — `odm-store`'s 49 unit/integration tests (incl. `rename`/`init`), the 3 existing store-lifecycle test files (`store_commit`/`store_status`/`store_sync`, unchanged behavior), and the new 16-fixture `store_set_remote.rs`, all pass. | |
