# Slice 06 (Store Lifecycle): `store set-remote`

> Per LEDGER-DISCIPLINE v2.0 (slice scale, §A). Evidence strength `asserted < attested < reproduced <
> reconciled`; a `done` row reaches ≥ `reproduced`. Code/fixtures class-(a): CDC reproduces by direct read;
> runtime (`cargo`/`clippy`/a live `store set-remote` + `store sync` run) attested→CI / operator.
> Five-iteration cap.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | **`set-remote <name>`**: validates the named remote exists in git (`git remote get-url`), writes `remote = "<name>"` to `[store]` in `odm.toml`, reports success; calling again with a different name overwrites; calling with a nonexistent remote errors clearly | fixture: set-remote with a valid remote → config updated; set-remote with an invalid remote → error exit 2; set-remote twice → second value wins | serious (the config command) | slice-doc D-2 | done | CDC-reproduced 2026-08-04: code review + `cargo test --test store_set_remote` (16/16 pass, cloud container). `set_remote_explicit` + `set_remote_explicit_nonexistent` + `set_remote_explicit_overwrite`. | `StoreLocation` gains `remote: Option<String>` (backward-compatible: `None` when absent). **D-5 approach: `toml_edit::DocumentMut`**, not `toml::Value` round-trip — preserves comments byte-for-byte; already a dependency. |
| F-2 | **`set-remote` (no arg, auto-detect)**: exactly one remote → uses it (same as explicit); zero remotes → clear error; multiple remotes → clear error listing the available names | fixture: repo with one remote → auto-detects; repo with no remote → error; repo with two remotes → error listing both | serious (the UX) | slice-doc D-1 | done | CDC-reproduced 2026-08-04: `set_remote_auto_one` (`auto_detected: true`) + `set_remote_auto_zero` + `set_remote_auto_multi`. | No interactive prompt. `worktree::list_remotes()` (new helper, follows `capture()`). |
| F-3 | **`sync` first-push**: when a remote is configured (or defaulted) and fetch succeeds but the upstream ref doesn't exist (branch never pushed), `sync` pushes the branch; local HEAD afterward matches remote HEAD; `--dry-run` reports "would push" but doesn't push | fixture: `store init` + `store set-remote` + `store commit` → `store sync --json` → action `"pushed"`, remote now has the commit; dry-run variant leaves remote unchanged | serious (the bootstrapping gap) | slice-doc D-4 | done | CDC-reproduced 2026-08-04: `sync_first_push` (bare remote branch absent before, present after) + `sync_first_push_dry_run` (branch still absent after dry-run, real sync then pushes). | `NoUpstream` arm splits: `fetch_ok` + no ref → first-push via `LocalAhead`; fetch-failed → true `NoUpstream`. |
| F-4 | **`sync` reads configured remote**: when `store.remote` is set in `odm.toml`, `sync` uses it instead of `DEFAULT_REMOTE`; when absent, falls back to `DEFAULT_REMOTE` ("origin") | fixture: set-remote to a non-"origin" name → sync uses it; store with no `remote` field → sync falls back to "origin" | serious (the config integration) | slice-doc D-2 | done | CDC-reproduced 2026-08-04: `sync_reads_configured_remote` (adversarial: only `"github"` remote, no `"origin"` — if sync used hardcoded default it would fail) + `sync_falls_back_to_default`. | Backward-compatible: existing stores without `remote` behave exactly as before. |
| F-5 | **`store init` auto-sets remote**: when bootstrapping a new store in a repo with exactly one remote, `write_locator` includes `remote = "<name>"`; multi-remote repos get no `remote` field; existing stores are unaffected | fixture: `init` in a single-remote repo → `odm.toml` has `remote`; `init` in a no-remote repo → no `remote` field; `init` in a two-remote repo → no `remote` field | moderate (convenience) | slice-doc D-3 | done | CDC-reproduced 2026-08-04: `init_auto_sets_remote` + `init_no_remote_no_field` + `init_multi_remote_no_field`. | Chose the "simpler alternative" the cc-prompt offered: inline `write!` in `write_locator()`. Correct — `init` writes a new `[store]` from scratch, so `toml_edit`'s format preservation doesn't apply. |
| F-6 | **`--json`** for `set-remote`: `{remote, url, auto_detected, branch, store_root}` — `auto_detected: true` when no argument was given | `--json` parses + fields correct on explicit, auto-detect, and error cases | serious (LLM ergonomics) | ODD-0023 | done | CDC-reproduced 2026-08-04: `set_remote_json` (all five fields asserted) + `set_remote_json_auto` (`auto_detected: true`). Error cases use `anyhow::bail!` (house pattern). | |
| F-7 | **No model drift**: CLI + `odm-store` plumbing only; no node-schema change; ODD-0022 unchanged; `status`/`commit` unmodified | cross-read: diff scope is CLI + store home + init (narrow); ODD cited if touched | correctness | ODD-0022 | done | CDC-reproduced 2026-08-04: diff scope confirmed narrow; no node-schema or ODD change. `status()` gained one line (reads `location.remote` — cc-prompt-recommended consistency fix). `rename.rs` gained `remote` threading (latent-bug fix, compiler-enforced). | |
| F-8 | **Clippy clean; no `unsafe`; new code covered** | clippy `-D warnings` exit 0; `! grep unsafe`; fixtures cover set-remote (explicit/auto/error), sync first-push, sync config-read, init auto-set | polish | CLAUDE.md | done | CDC-reproduced 2026-08-04: `cargo clippy -p odm-store -p odm-cli` clean; `grep unsafe` on changed files: none; 16 new fixtures + full workspace `cargo test` (~770 pass, 2 pre-existing `edge_cases` failures confirmed identical without s06 patch). | |
