# Slice 03 (Store Lifecycle): `store sync`

> Per LEDGER-DISCIPLINE v2.0 (slice scale, §A). Evidence strength `asserted < attested < reproduced <
> reconciled`; a `done` row reaches ≥ `reproduced`. Code/fixtures class-(a): CDC reproduces by direct read;
> runtime (`cargo`/`clippy`/a live `store sync` run) attested→CI / operator. Five-iteration cap.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | **Pull (ff-only)**: when the upstream has commits the local branch lacks, `store sync` fetches and fast-forwards the local branch — never creates a merge commit; the store root matches what upstream has | fixture: bare-repo remote with commits ahead → `store sync` → local branch advanced, commit history linear, worktree updated | serious (the pull direction) | arc-plan SL-3 | done | `store_sync.rs::upstream_ahead_pulls_and_fast_forwards` + `pull_reports_in_plain_text_too` — a real bare-repo remote, advanced independently via a throwaway second clone (`advance_bare_repo`), then `store sync --json` → `action: "pulled"`, local `HEAD` asserted equal to the bare repo's branch tip. | **Real defect found and fixed, not silently worked around:** `worktree::merge_ff_only(worktree_dir)` ran a bare `git merge --ff-only` with no target ref, which relies on the branch's `branch.<name>.merge` upstream-tracking config — a value **nothing in `odm-store` ever sets** (not `store init`'s bootstrap/attach arms, not a plain `push`/`fetch` without `-u`). Every real fixture run failed with `fatal: No remote for the current branch`. This is the *first* end-to-end fixture that ever exercised the FastForward branch of `init::sync()`'s own decision table too — its own test suite only unit-tests `sync_action()`'s pure logic, never the actual merge call. Fixed by widening `merge_ff_only`'s signature to take an explicit `upstream_ref: &str` and pass it to `git merge --ff-only <ref>`; updated both call sites (`init.rs::sync()` and this slice's `sync_cmd`). This is a second `odm-store` change beyond the cc-prompt's anticipated sole addition (`push()`) — flagged here per F-7's Notes, not silently absorbed. |
| F-2 | **Push**: when the local branch has committed work upstream lacks, `store sync` pushes to the remote — the remote gains the local commits; never force-pushes | fixture: local commits ahead of bare-repo upstream → `store sync` → the bare repo now has those commits (verify via `rev_parse` on the bare side) | serious (the push direction — the new capability) | arc-plan SL-3 | done | `store_sync.rs::local_ahead_pushes_to_the_remote` + `push_reports_in_plain_text_too` — commits locally, confirms the bare remote's branch ref differs beforehand, runs `store sync --json` → `action: "pushed"`, then asserts the bare repo's branch ref now equals the local commit that was pushed. | `worktree::push()` — new, follows `fetch()`'s `run()` pattern exactly: `git push <remote> <branch>`. Never `--force`, by construction (the arg list has no force flag, and there is no code path that could add one). |
| F-3 | **Divergence stops**: when both sides have commits the other lacks, `store sync` reports the situation with a clear affordance and changes **nothing** — no merge, no rebase, no push, no pull; the local branch and the remote are bit-identical before and after | fixture: push one commit locally AND add one commit directly on the bare remote → `store sync` → action is `diverged`, local `HEAD` unchanged, remote `HEAD` unchanged | serious (ODD-0022 discipline) | ODD-0022 §6 | done | `store_sync.rs::diverged_store_changes_nothing` + `diverged_reports_in_plain_text_too` — advances the bare remote independently AND commits locally, snapshots both branch tips, runs `store sync --json` → `action: "diverged"`, then asserts **both** tips are byte-identical to their pre-run values (not just that the action string is right). Also doubles as F-2's never-force proof: `sync` classifies `Diverged` *before* attempting any push, so the remote is never even approached. | |
| F-4 | **No-op cases**: up-to-date → "nothing to do" (exit 0); no upstream configured → "no upstream" (exit 0, not an error) | fixtures: synced store → clear message, exit 0; store with no remote → clear message, exit 0 | serious | slice-doc | done | `store_sync.rs::up_to_date_is_a_no_op` (`action: "up-to-date"`, `ahead: 0, behind: 0`) + `no_upstream_is_not_an_error` (`action: "no-upstream"`, `upstream: null`, exit 0). Mirrors `status`'s F-4 handling exactly — the `SyncAction::NoUpstream` arm falls out of the same `rev_parse` → `None` path. | |
| F-5 | **`--dry-run`**: fetches + compares (so the preview matches the real run) but **does not** merge or push; reports what *would* happen | fixture: upstream ahead → `store sync --dry-run` → reports "would pull" but local `HEAD` unchanged; local ahead → `--dry-run` → reports "would push" but remote unchanged | serious | ODD-0023 | done | `store_sync.rs::dry_run_reports_would_pull_but_leaves_local_untouched` + `dry_run_reports_would_push_but_leaves_remote_untouched` + `dry_run_reports_in_plain_text_too` — each asserts the relevant side (local `HEAD` for pull, bare remote's tip for push) is byte-identical before/after the dry run, then runs a **real** sync afterward and confirms it *does* now apply the previewed change — proving the preview matched reality, not just that nothing happened. Fetch happens unconditionally (D-2); the `if !dry_run` guard wraps only `merge_ff_only`/`push`. | |
| F-6 | **`--json`**: `{action, dry_run, branch, store_root, upstream: {ref, ahead, behind} \| null}` | `--json` parses + fields correct on push, pull, up-to-date, diverged, no-upstream, and dry-run runs | serious (LLM ergonomics) | ODD-0023 | done | `store_sync.rs::json_shape_is_correct` + every fixture above asserts individual fields via `--json`. Implemented exactly per spec: `SyncJson{action, dry_run, branch, store_root, upstream: Option<SyncUpstreamJson>}`, `SyncUpstreamJson{ref, ahead, behind}` (`ref` via `#[serde(rename = "ref")]` on a `reference` field, same pattern as `StatusJson`). `action` values: `pushed` \| `pulled` \| `up-to-date` \| `diverged` \| `no-upstream` — reflects the classification (what happened, or under `--dry-run`, what it calls for), independent of a separate `sync_action_str` from `status`'s own `action_str` (deliberately different vocabulary: `status` describes a *state*, `sync` describes an *outcome*). `ahead`/`behind` are post-sync (both zeroed after a real push/pull; unchanged under `--dry-run` or for `Diverged`/`UpToDate`). | |
| F-7 | **No model drift**: CLI + `odm-store` plumbing only; no node-schema change; ODD-0022 amended only if a line is genuinely needed | cross-read: diff scope is CLI + store; ODD cited if touched | correctness | ODD-0022 | done | Diff: `odm-cli/src/lib.rs` (`StoreCommand::Sync` variant + dispatch arm), `odm-cli/src/store_cmd.rs` (`sync_cmd` + `SyncJson`/`SyncUpstreamJson`/`sync_action_str`), `odm-cli/tests/store_sync.rs` (new). `odm-store` changes: `worktree::push()` (new, as anticipated) **and** `worktree::merge_ff_only`'s signature widened to take an explicit `upstream_ref` (see F-1's Notes — a real pre-existing bug, not scope creep for its own sake; flagged rather than silently absorbed per the working agreement). Both call sites of `merge_ff_only` updated (`init.rs::sync()`, this slice's `sync_cmd`); `odm-store`'s own 49-test suite (incl. `init::tests`) still green after the change. No ODD-0022 amendment — the push/pull/divergence discipline is exactly what the model already specifies; this slice implements it, doesn't change it. | |
| F-8 | **Clippy clean; no `unsafe`; new code covered** | clippy `-D warnings` exit 0; `! grep unsafe`; fixtures cover push/pull/diverged/up-to-date/no-upstream/dry-run/json | polish | CLAUDE.md | done | `make lint` (clippy `-D warnings` + rustfmt `--check`) clean; `grep unsafe` on every new/changed file finds none; `make test` (full workspace incl. doctests) green. All 13 `store_sync.rs` fixtures pass, covering pull/push/diverged/up-to-date/no-upstream/dry-run(×2)/json/no-store-section, each with both a `--json` and a plain-text variant where applicable. | |
