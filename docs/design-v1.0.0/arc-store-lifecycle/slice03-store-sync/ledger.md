# Slice 03 (Store Lifecycle): `store sync`

> Per LEDGER-DISCIPLINE v2.0 (slice scale, §A). Evidence strength `asserted < attested < reproduced <
> reconciled`; a `done` row reaches ≥ `reproduced`. Code/fixtures class-(a): CDC reproduces by direct read;
> runtime (`cargo`/`clippy`/a live `store sync` run) attested→CI / operator. Five-iteration cap.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | **Pull (ff-only)**: when the upstream has commits the local branch lacks, `store sync` fetches and fast-forwards the local branch — never creates a merge commit; the store root matches what upstream has | fixture: bare-repo remote with commits ahead → `store sync` → local branch advanced, commit history linear, worktree updated | serious (the pull direction) | arc-plan SL-3 | open | | Reuses `worktree::fetch` + `worktree::merge_ff_only` — the same path `init::sync()` takes. The bare-repo remote fixture follows s02's `with_fetched_upstream` pattern. |
| F-2 | **Push**: when the local branch has committed work upstream lacks, `store sync` pushes to the remote — the remote gains the local commits; never force-pushes | fixture: local commits ahead of bare-repo upstream → `store sync` → the bare repo now has those commits (verify via `rev_parse` on the bare side) | serious (the push direction — the new capability) | arc-plan SL-3 | open | | `worktree::push()` is new — follows `fetch()`'s `run()` pattern: `git push <remote> <branch>`. Never `--force`. |
| F-3 | **Divergence stops**: when both sides have commits the other lacks, `store sync` reports the situation with a clear affordance and changes **nothing** — no merge, no rebase, no push, no pull; the local branch and the remote are bit-identical before and after | fixture: push one commit locally AND add one commit directly on the bare remote → `store sync` → action is `diverged`, local `HEAD` unchanged, remote `HEAD` unchanged | serious (ODD-0022 discipline) | ODD-0022 §6 | open | | The `SyncAction::Diverged` arm — the same decision table `init::sync()` and `status` use. The fixture must assert no mutation on either side, not just the right action string. |
| F-4 | **No-op cases**: up-to-date → "nothing to do" (exit 0); no upstream configured → "no upstream" (exit 0, not an error) | fixtures: synced store → clear message, exit 0; store with no remote → clear message, exit 0 | serious | slice-doc | open | | Mirrors `status`'s F-4 handling. |
| F-5 | **`--dry-run`**: fetches + compares (so the preview matches the real run) but **does not** merge or push; reports what *would* happen | fixture: upstream ahead → `store sync --dry-run` → reports "would pull" but local `HEAD` unchanged; local ahead → `--dry-run` → reports "would push" but remote unchanged | serious | ODD-0023 | open | | Fetch happens even under `--dry-run` (D-2) — same rationale as `init::sync()`. The `--dry-run` guard wraps `merge_ff_only` and `push`, never `fetch`. |
| F-6 | **`--json`**: `{action, dry_run, branch, store_root, upstream: {ref, ahead, behind} \| null}` | `--json` parses + fields correct on push, pull, up-to-date, diverged, no-upstream, and dry-run runs | serious (LLM ergonomics) | ODD-0023 | open | | `action` values: `pushed` \| `pulled` \| `up-to-date` \| `diverged` \| `no-upstream`. `upstream` is `null` only when `action` is `no-upstream`. |
| F-7 | **No model drift**: CLI + `odm-store` plumbing only; no node-schema change; ODD-0022 amended only if a line is genuinely needed | cross-read: diff scope is CLI + store; ODD cited if touched | correctness | ODD-0022 | open | | `worktree::push()` is the sole new `odm-store` function; the rest is CLI orchestration over existing plumbing. |
| F-8 | **Clippy clean; no `unsafe`; new code covered** | clippy `-D warnings` exit 0; `! grep unsafe`; fixtures cover push/pull/diverged/up-to-date/no-upstream/dry-run/json | polish | CLAUDE.md | open | | |
