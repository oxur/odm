# Slice 01 — Store resolution + two-config split (ledger)

> Per LEDGER-DISCIPLINE v2.0 §A (slice tier). Rows are the grep-verifiable **steps**; each reaches a
> final status (`done` / `deferred` / `no-op`) before the slice closes. Evidence strength
> `asserted < attested < reproduced < reconciled`; cargo/executable rows are attested-by-CC →
> reproduced-on-CI. Closer ≠ verifier (CDC writes `cdc-verification.md`). Feeds arc row **SH-1**.

| ID | Criterion | Verify | Significance | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|----------|-------|
| L-1 | `[store]` section parses from `odm.toml` — `worktree_base` / `worktree_name` / `branch_name`, all **optional** with defaults `.worktrees` / `odm` / `odm` | unit test; `grep` the config struct | serious | **done** | `odm-store/src/home.rs` `StoreLocation` + unit tests `test_store_section_parses_with_every_field`, `test_store_section_fields_default_individually` | the locator contract (ODD-0022 §4.2); all three fields `#[serde(default)]`, so a bare `[store]` resolves to `.worktrees`/`odm`/`odm` |
| L-2 | With `[store]` present, store root resolves to `<repo>/<worktree_base>/<worktree_name>` | unit test | serious | **done** | unit `test_store_root_resolves_to_the_worktree_when_directed`, `test_store_root_honours_custom_names` |  |
| L-3 | **Back-compat:** with **no** `[store]`, store root = repo root (unchanged) | unit test | serious | **done** | unit `test_store_root_is_the_repo_root_without_a_store_section`, `test_no_locator_at_all_resolves_to_the_starting_directory`; integration `without_a_store_section_everything_stays_at_the_repo_root` | zero behaviour change for existing repos; reproduced on odm's own repo — `check` green at 60 nodes, `nodes/` still at the root, no `.worktrees/` invented |
| L-4 | Two-stage load: operational config (`gate-sets`, `display`, `docs_directory`, author) loads from **`config.toml`** at the resolved store root | unit + integration | serious | **done** | unit `test_operational_config_comes_from_the_store_when_present`; integration `gate_sets_are_read_from_the_stores_config_toml`, `the_stores_config_wins_over_the_locator` | the operational half of the split; precedence proven by a gate name only `config.toml` defines |
| L-5 | **Back-compat:** `config.toml` absent → operational config falls back to `odm.toml` | unit test | serious | **done** | unit `test_operational_config_falls_back_to_the_locator`; integration `operational_config_falls_back_to_the_locator_when_the_store_has_none` | existing repos keep working before migration; the state every repo sits in between this slice and the C-5 cutover |
| L-6 | Node reads/writes use the **resolved** store root (not the repo root) | integration: `odm list`/`check` on a hand-placed `.worktrees/odm/` store | serious | **done** | integration `nodes_are_written_into_the_resolved_store_not_the_repo_root`, `a_custom_worktree_name_is_honoured` | the payoff — commands operate on the configured home; asserts both halves — nodes land in the worktree **and** nothing is written at the repo root |
| L-7 | **No git-worktree ops / no `git` subprocess** introduced in this slice | `grep -rE 'Command::new\("git"\)\|worktree' crates` over the diff → none | correctness | **done** | `grep -rnE 'Command::new' crates` → only `odm-reconcile/src/shell.rs` (the pre-existing shell *probe*); `git.rs` untouched | the scope boundary (git shell-out is slice 02); `worktree` appears only as config field names, test fixtures and prose |
| L-8 | `cargo build` / `test` / `clippy --all-targets -- -D warnings` / `fmt` green; no `unsafe` | CC attest (local 1.85+) → CI | serious | **done** | `cargo build` / `test --all-features --workspace` (**54 binaries, 0 failed**) / `clippy --all-targets -- -D warnings` / `fmt --check` — all green; no `unsafe` | attested-by-CC → reproduced-on-CI; attested-by-CC (local 1.85+) → reproduced-on-CI |
| L-9 | `odm check` green in **both** modes (with and without `[store]`) | integration | serious | **done** | integration `check_is_green_against_the_resolved_store` + `without_a_store_section_everything_stays_at_the_repo_root`; plus odm's own corpus green at 60 nodes | both modes |

**Close:** per-row walk in `closing-report.md` (CC) + `cdc-verification.md` (CDC), with the
**bubble-up to `arc-plan.md`** (did slice 01 deliver SH-1; anything the arc-plan didn't anticipate;
the silent-drop diff).
