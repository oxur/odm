---
id: 01KYP5FYD4PHWEP2MW2SJS7AWB
number: 509907700
type: artifact
schema: artifact/v1.1
name: 'Slice 10 (Migration Fidelity): Coverage live run'
created: 2026-07-28
updated: 2026-07-29
origin: planned
reserved: false
source:
  paths:
  - docs/design-v1.0.0/arc-migration-fidelity/slice10-coverage-live-run/ledger.md
  class: ledger
  normalization: trim+lf
  migrated_by: odm-migrate/1.0.0
  migrated_on: 2026-08-01
edges:
  part_of: 01KYP5FRXJFZ3FSF123XS3FD9G
---
# Slice 10 (Migration Fidelity): Coverage live run

> Per LEDGER-DISCIPLINE v2.0 (slice scale, §A). Evidence carries a **strength**
> (`asserted < attested < reproduced < reconciled`); a `done` row reaches ≥ `reproduced` at its scale.
> **This slice mutates the live `odm` store** (a large mint + a gate flip) as **one revertible commit**
> after a clean, adjudicated dry-run. **Snapshot/revert + dry-run-first are HARD gates.** The live rows
> are class-(b) — the committed store is the evidence; CDC reproduces by direct read. Five-iteration cap.

## Ledger

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-1 | **Pre-flight**: `release/1.0.x` green (s09 merged); `odm` store worktree clean; **known-good SHA + before-manifest captured** | `make check` green; `git -C .worktrees/odm status --porcelain` empty; record SHA (`7226797`) + sha256 composite over all node files | serious (revert anchor) | s07 protocol | done | reproduced | `cargo test --workspace`/`clippy -D warnings` green throughout; `git -C .worktrees/odm status --short` empty; SHA `7226797`; before-manifest `24b1fa4f13df638aa3da6b2db449425ac971003ddfe81773f5a18c769b24ef1d` over 78 node files. Re-captured (identical) after the mid-run code fixes forced a discard-and-redo. |
| F-2 | **Dry-run adjudicated**: creates = s01's supporting-doc inventory (count + identity); modifies = exactly the 14 design/research nodes; **0 unexpected change** to the 62 existing source-bearing nodes / project / retired; store fingerprint stable after dry-run | `odm migrate --dry-run` → preview counts; diff the create/modify sets against s01 `coverage-report.md` + the 14-node list; re-hash store = unchanged | serious (the fire gate) | s07/s08 adjudicate | done | reproduced | Adjudicated against a **fresh** live coverage scan (s01's own inventory was 3 live-run slices stale) rather than the literal s01 numbers: self-host 2 creates (slice09/10's own plan nodes, drawn after `7226797`); `--artifacts` 254 mints (matches `ledger 53 + cc-prompt 64 + cdc-verification 56 + closing-report 62 + other 19`, a fresh `--coverage` cross-check); legacy `docs/design` 12 reconciled + **2 drifted (disclosed, see F-6)** + 4 created (ODD-0022…0025, authored but never migrated); `--notes` 31 mints. Every dry-run count matched the real run exactly; fingerprint unchanged after each dry-run. |
| F-3 | **Artifact mint-all (§2.6)**: every supporting `.md` under the scan root gets a faithful 1:1 `artifact` node — `ledger`/`cc-prompt`/`cdc-verification`/`closing-report`/ADR/amendment/UAT **+ every report incl. `coverage-report.md`**; no exemption | direct read of the committed store: one `artifact` node per supporting doc; count matches s01 inventory; `coverage-report.md` has a node | serious | ODD-0025 §2.6 | done | reproduced | 254 `artifact` nodes committed at `355404a`; direct read confirms `coverage-report.md` (`slice01-coverage-discovery/coverage-report.md`) has a node (id `01KYP5FT67627AVX4MW5YPQTAW`). Cross-checked structurally (script, not spot-check): every one of 251 artifact-family files under `docs/design-v1.0.0` maps to exactly one node via `source.paths`. |
| F-4 | **Mint fidelity**: every minted `artifact` body byte-matches its source under `normalize` (trim+lf); §2.1 hard gate held on every mint | direct read: recompute the body-hash gate on the minted nodes (resolve relative `source.paths` → compare) | serious | ODD-0025 §2.1 | done | reproduced | The mint ran to completion with 0 `BodyHashMismatch` (the gate is hard — any failure aborts `mint_artifacts` entirely, unlike the design/research backfill's per-node skip, F-6); `artifact.rs`'s own `verify_body_hash` call is unconditional per mint. |
| F-5 | **Containment (§2.5)**: each `artifact` is `part_of` its nearest modeled scale — per-slice → its slice, arc-/chunk-level → its arc | direct read: sample per-slice + arc-/chunk-level artifacts → correct `part_of` edge | serious | ODD-0025 §2.5 | done | reproduced | Full structural cross-check (not a sample): 250 of 251 artifact-family files resolve to exactly the expected slice/arc parent. The one exception (`arc06-migrate-self-host/slice05-uat-cli-feedback/ledger.md` → falls through to the arc) is not a defect: that slice (`#1605`) is a **retired, sourceless tombstone** predating the source model (its own retirement note documents it as a stale directory from an abandoned plan revision, v1.8→v1.9) — the containment index correctly cannot place it and falls to the next real ancestor. |
| F-6 | **Design/research `source` backfilled (F7)**: the 14 `design`/`research` nodes now carry `source`; none flagged orphan (optional containment §2.7) | direct read: the 14 nodes source-bearing; `check`/orphan does not flag a top-level doc node | serious | arc-plan MF-3 / F7 | done | reproduced | **12 of 14 backfilled**; the other 2 (ODD-0013, ODD-0020) have **drifted** from their legacy source since original migration (both actively amended throughout this rebuild) — `backfill_source` correctly leaves a drifted node completely untouched (no `source`, no body change) rather than silently backfilling over a mismatch, per its s09 hard-gate design (extended this slice to skip-and-continue per node rather than abort the whole batch, `crates/odm-migrate/src/mapping.rs::Drifted`). **Disclosed, not silently dropped** — reconciling the drift is `s12`'s job. `odm migrate docs --coverage`'s "missing provenance" count (4 = project `#1000` + retired `#1605`, both correctly excluded by design, + the 2 drifted) confirms no other gap. None of the 14 orphan-flagged: optional containment (§2.7) already held structurally (s09 F-8). |
| F-7 | **Coverage enforced + green**: doc-coverage gate **on** in the live `.worktrees/odm/config.toml`; `odm check` **exit 0** with 0 uncovered `.md`; a seeded uncovered doc would fail | `odm check` exit 0 (coverage enforcing); `odm migrate --coverage` → 0 uncovered; (fixture/negative: seeded uncovered → Error, from s09) | serious (MF-1/MF-6) | ODD-0025 §5 / arc MF-6 | done | reproduced | `[coverage] scan_root = "docs"` added to `.worktrees/odm/config.toml` in the same commit as the mint. `odm check` on the committed store: exit 0, 0 errors, the same 8 pre-existing (unrelated) warnings as before this slice — no red window. `odm migrate docs --coverage`: **371/371 docs covered, 0 uncovered**. Scope note: `scan_root = "docs"` covers the *whole* `docs/` tree, wider than the original ledger's plan (which anticipated a narrower scope before the operator-directed additions — F-11/F-12 below — made full-tree coverage achievable). |
| F-8 | **No collateral + idempotent**: the 62 pre-existing source-bearing nodes, project (`#1000`), retired (`#1605`) unchanged; ids/bodies/schema intact; `orient`/`rollup` byte-stable; re-run idempotent (0 created / 0 reconciled) + cross-root stable | direct read: last-touch commits of project/retired unchanged; existing-node diff = none; 2× `orient`/`rollup` identical; second `migrate` run 0/0 | serious | slice-doc | done | reproduced | `git log -1` on both `#1000`'s and `#1605`'s node files: still `b45b122` (the pre-s07 cutover commit) — untouched. `orient`/`rollup --dry-run` byte-identical across 2 runs. Re-running all four commands (self-host, `--artifacts`, legacy `docs/design`, `--notes`) a second time: `0 created`/`0 minted`/`0 reconciled` across the board (the 2 disclosed drifted nodes correctly re-report each time, not a non-idempotence — nothing was written for them either run). |
| F-9 | **One revertible commit; rollback discipline; no downstream pulled forward** | single `odm`-branch commit atop `7226797`; `git reset --hard 7226797` documented; no synthesis (s11) / reconcile (s12) work; no capability code beyond the live gate wiring | serious | LEDGER-DISCIPLINE / operator | done | reproduced | Single commit `355404a` on `odm`, atop `7226797` (304 files, 49864 insertions/35 deletions). Undo: `git reset --hard 7226797`. No synthesis/reconcile-runner code touched. **Capability grew beyond the original "gate wiring only" plan** — F-11…F-15 below, all operator-directed, disclosed as deviations, not silently absorbed. |
| F-10 | **No model drift; clippy/unsafe clean on any code touched** | cross-read: `source` still the identity axis, body-hash gate unchanged, `artifact` per §2.5; clippy exit 0 + no `unsafe` on any changed file; ODD amended-not-worked-around if a line was needed | polish/correctness | CLAUDE.md / ODD-0025 | done (iteration 1 correction) | reproduced | `clippy --workspace --all-targets --all-features -- -D warnings` clean at every checkpoint; `grep unsafe` on every file this slice touched → none; no ODD edited. `source` unchanged as the identity axis; the body-hash gate unchanged (still hard, still trim+lf). **Iteration 1 correction (2026-07-29):** CDC verification falsified this row's "no model drift" claim — 4 nodes (#22–25) carried **absolute** `source.paths`, exactly the portability invariant this row asserted was intact. Root cause: `mapping::build_node` (the design/ODD import path) was the one creation-path seam never routed through `fidelity::relativize`, unlike `artifact.rs`/`notes.rs`/`backfill_source`. Fixed and now durably enforced (F-16/F-17 below) — re-verified: 0 absolute `source.paths` remain, clippy/unsafe still clean on every iteration-1 file. |
| F-11 | **Infrastructure-file exclusion**: `docs/design/index.md` and anything under `templates/` excluded from doc-coverage outright (never ingested, by design) — operator decision, surfaced when the live scan revealed 6 permanently-uncoverable `odd`-classified files | `cargo test` → both files show `covered: true`, basis "excluded infrastructure file"; live `--coverage` no longer lists them | correctness | operator (this slice) | done | reproduced | `coverage.rs::doc_coverage` now checks `legacy::is_excluded` (made `pub(crate)`) before the class-based fallback. Fixture test `coverage_excludes_index_and_template_files`. Confirmed live: `--coverage`'s "odd" bucket dropped from 6 to 4 (the 2 excluded files gone, the 4 genuinely-unmigrated ODDs remain, then get imported by F-2's legacy-design pass). |
| F-12 | **Note mint-all**: every `docs/dev/**` file becomes a `NodeType::Note`, tagged by immediate subdirectory, deliberately uncontained — operator decision, general `odm-migrate` capability (not project-specific; the legacy `Config::dev_directory` field, `./docs/dev` default) | `cargo test` → mint-all, tag derivation, uncontained, idempotent, dry-run-safe; live: 31 notes minted (25 untagged + 5 `research` + 1 `skill`), 0 `part_of` | serious (closes the `docs/dev` gap `--group reference` alone can't) | operator (this slice) | done | reproduced | New `crates/odm-migrate/src/notes.rs::mint_notes`. 4 unit + 4 integration tests. Live: 31 notes committed at `355404a`, confirmed uncontained (`part_of: None` on every one) and tagged correctly (`research`/`skill` subdirectories; direct children untagged). |
| F-13 | **Git-derived `created`/`updated`**: every creation/reconcile path stamps real history (RH F-20's mechanism, `first_commit_date`/`last_commit_date`) instead of "the day this ran" — operator-identified regression, previously flagged and never fixed | `cargo test` → 2 new `fidelity.rs` unit tests against a real `TempDir` git repo with controlled commit dates; falls back to `fallback` only when git has no record | correctness | operator (this slice, RH F-20 origin) | done | reproduced | New `fidelity::git_derived_dates`, wired into `artifact.rs`, `notes.rs`, `selfhost.rs` (`build_node` + `reconcile_source`), `mapping.rs` (`build_node` fallback layer + `backfill_source`). Gate-evidence dates (`set_gate`/`affirm_decomposed`) deliberately still stamp "today" — a different concept (when this run asserted the evidence). Live spot-check: a minted artifact from `slice01-coverage-discovery/coverage-report.md` (first committed 2026-07-27 per `git log`) shows `created: 2026-07-27`, not `2026-07-29` (the mint's run date). **Known residual, accepted, not chased further** (operator call): this repo's own git history may not reflect true original authoring dates for content extracted from the Oxur monorepo — `--follow` testing found no rename-tracking discrepancy *within* this repo's history, so the residual (if any) is a cross-repo-extraction question outside what `git log` in this repo can recover, and outside this slice's scope. |
| F-14 | **Decomposition-drift fix**: `check_decomposition` (stub/undecomposed-parent/drift) counts only `is_work()` children, not any `part_of` child — surfaced live when the mint attached artifacts to arcs that had already affirmed `decomposed: complete` | `cargo test` → 2 new `recompose.rs` tests: document-family children ignored, a genuinely new slice still flags drift; live: 0 `decomposition-drift` errors post-mint | serious (would have blocked F-7's green `check`) | operator (this slice) | done | reproduced | `integrity()` now builds an `id → NodeType` map; `check_decomposition` filters `kids` to `is_work()` throughout. Live: `odm check` post-mint shows 0 `decomposition-drift` findings (4 arcs would have wrongly flagged without this fix — confirmed by reproducing the failure before the fix, in the same session). |
| F-15 | **`odm list` promotion**: an `artifact` `part_of` a slice or an arc renders tree-nested under it (not "orphaned" in the flat reference group), with a distinct cyan-leaning colour — operator decision, discovered while reviewing the live mint's `node list` output | `cargo test` → 9 new `listview.rs` integration tests (slice/arc promotion, depth, colour, `--group` respecting promotion, an uncontained artifact staying unpromoted); live cross-check: 250/251 artifact-family files nest exactly where expected | polish/correctness (readability) | operator (this slice) | done | reproduced | Pure display logic — no store mutation, no re-fire needed. `listview.rs::promoted_artifacts` (structural: `part_of` type ∈ {Slice, Arc}) threaded through `Group::holds`, `work_tree`, `type_color` (RGB `150,224,248`, same S/L family as slice's blue, hue-shifted toward cyan). Live-verified via `node list --all` on the committed store. |

## Iteration 1 — source-path portability regression (CDC finding)

> CDC verification of the s10 close (rows F-1…F-15 above) reproduced the live store and found a silent
> regression: 4 nodes minted this slice carried absolute `source.paths` — CDC v2.8 Finding 1's exact bug,
> reintroduced on the design/ODD import seam. `check` passed green only because the coverage matcher
> relativizes before comparing. Fixed the seam, added a durable enforced invariant, and fired the
> corrective live rewrite — all behind the s07/s08 protocol.

| ID | Criterion | Verify | Significance | Origin | Status | Evidence | Notes |
|----|-----------|--------|--------------|--------|--------|----------|-------|
| F-16 | **Seam fixed**: the design/ODD import path (`mapping::build_node`) relativizes `source_path` before storing, identical to every other creation path | `cargo test` → new regression test `migrate_stores_relative_canonical_source_paths` | serious (root cause) | CDC finding, iteration 1 | done | reproduced | `mapping.rs::build_node` now calls `crate::fidelity::relativize(anchor, source_path)` before `build_source` — the one missing call site (`artifact.rs`/`notes.rs`/`backfill_source` already did this). `crates/odm-migrate/tests/source_path_portability.rs::migrate_stores_relative_canonical_source_paths` green on `release/1.0.x`. |
| F-17 | **Durable invariant enforced**: `check` gains an unconditional `absolute-source-path` Error rule — any `source.paths` entry that is absolute or `.worktrees/`-anchored, on any node type, fails `check`, no config gate | `cargo test` → 4 new `odm-core` unit tests + 3 new `odm-cli` end-to-end fixtures (seeded absolute → Error naming the node, `.worktrees/`-anchored → Error, relative → green, no source → unaffected) | serious (prevents recurrence) | CDC finding, iteration 1 | done | reproduced | `odm_core::check::Violation::AbsoluteSourcePath` + `check_source_paths`, wired into `content_validity` (always runs — unlike `[coverage] scan_root`, there is no legitimate absolute case, so no config gate). CLI: `absolute-source-path` code, Error severity (default arm), fix affordance. `odm-core/tests/check.rs::content::{absolute_source_path_is_flagged_as_error, worktrees_anchored_source_path_is_flagged_as_error, relative_source_path_is_green, a_node_with_no_source_is_unaffected}`; `odm-cli/tests/check_source_path_portability.rs` (3 end-to-end tests). |
| F-18 | **Reproduce-the-bug evidence**: with the guard in place but before the live rewrite, `odm check` against the live store flags exactly the 4 known nodes (#22/#23/#24/#25) and no others; after the rewrite, 0 | direct read: `odm check` output before/after the live commit | serious (proves the guard catches the real regression, not just fixtures) | CDC finding, iteration 1 | done | reproduced | Before (`355404a`, freshly-built binary): `grep -c absolute-source-path` = 4, naming exactly `#22`/`#23`/`#24`/`#25` — no others across the 369-node corpus. After (`2fc25f5`): 0. The 2 pre-existing `uncovered-doc` findings (this iteration's own two planning docs, `cc-prompt-iteration1.md`/not-yet-written `closing-report.md`, not yet artifact-minted) are unchanged before/after — unrelated to this regression, disclosed not silently absorbed. |
| F-19 | **Live rewrite**: the 4 nodes' `source.paths` corrected in place — path string only, ids/bodies/schema/dates unchanged — as one revertible commit atop `355404a`, behind snapshot→dry-run→adjudicate→fire→verify | `git diff 355404a..2fc25f5` = 4 files, 1 line each; dry-run leaves the store fingerprint unchanged; post-fire node count unchanged (369); `orient` byte-stable across 2 runs; idempotent re-run (`0 path(s) canonicalized` second time) | serious (the actual fix) | CDC finding, iteration 1 | done | reproduced | New `mapping::canonicalize_source_paths` — `backfill_source`'s complement (that pass only ever touches *sourceless* nodes; these 4 already had a malformed one). Snapshot fingerprint `be63aa49b738eb0eadb7b9cf2f4d6df1212a0ab7ce202ca745e4f267e927b485` (369 files) unchanged after `--dry-run`; dry-run adjudicated exactly **4 canonicalize / 0 create / 0 upgrade** (`odm migrate docs/design --dry-run`); fired as commit `2fc25f5` on `odm` atop `355404a`; `git diff` confirms exactly one `source.paths` line changed per file (`#22`/`#23`/`#24`/`#25`), nothing else touched; node count 369→369; `odm orient` byte-identical across 2 runs; second `odm migrate docs/design` reports `0 path(s) canonicalized`. Coverage after: 371 covered (unchanged from s10's close), 373 total (2 higher — this iteration's own 2 new planning docs, disclosed at F-18, not yet artifact-minted; out of scope here). Undo: `git reset --hard 355404a`. |
| F-20 | **Optional s09 companion (ODD-0025 §4 wording) — explicitly skipped, flagged** | n/a — a disclosed disposition, not a criterion met | polish | cc-prompt optional (skip-if-not-clean allowed) | deferred | asserted | The cc-prompt authorized folding in a one-line ODD-0025 §4 amendment (`artifact/v1.0`→delivered `artifact/v1.1`) *if trivial*. Textually trivial, but editing that ODD's body would introduce fresh, silent body drift on live node **#25** — one of the exact 4 nodes this iteration's hard gate requires stay byte-identical. Skipped per the cc-prompt's own "skip and flag" allowance; left for the operator or a later slice (s12's reconcile scope is the natural home) rather than folded in here. |
| F-21 | **Disclosed finding: `ROLLUP.md` stale on `release/1.0.x`, unrelated to this regression** | n/a — a disclosed finding, not a criterion met | polish | discovered during F-19 verification | deferred | asserted | Running `odm rollup` for the F-19 byte-stability check regenerated the committed `ROLLUP.md` with a large diff (1042 insertions) — it predates this session's whole mint-all + RH C-2 taxonomy restamp (shows `odd #9` etc., not the current `design #9`/`research #11`), stale since at least s10's own close, not caused by this iteration. Reverted the incidental regeneration (`git checkout -- ROLLUP.md`) to keep this iteration's `release/1.0.x` diff scoped to its own changes. Regenerating it for real is a separate, disclosed follow-up (a natural s11/s12-adjacent housekeeping item), not silently done here. |

## What Worked

- **Fresh live adjudication beat the literal s01 inventory.** s01's coverage-report.md was three live-run
  slices stale by the time s10 fired; re-scanning `docs --coverage` immediately before the dry-run gave
  an accurate, self-consistent adjudication baseline (and caught the 2 new slice09/10 plan nodes, the 4
  unmigrated ODDs, and the true artifact-family count) that a literal diff against the old report would
  have missed or misreported.
- **Discovering the drift-skip need, the date-derivation gap, the decomposition-drift interaction, and the
  containment display gap all happened *during* dry-run/verify, before the irreversible commit** — each
  was fixed, tested, and the whole sequence re-run from a clean discard, rather than committing first and
  patching after. The one-clean-commit discipline held even though the path to it took five separate
  code fixes.
- **The operator's direct review of the live `node list` output caught two real gaps** (the date question,
  the artifact-orphaning display question) that no amount of fixture testing would have surfaced — a
  concrete case for looking at the actual rendered result on a real, large corpus before calling a slice
  done, not just trusting green tests.
- **A scripted, whole-corpus structural cross-check** (250/251 files, not a hand-picked sample) caught and
  explained the one exception cleanly, building real confidence in the containment logic beyond what
  fixture tests over a handful of synthetic cases could.

## Closure

**Base close** (F-1…F-15): store commit `355404a` (`odm` branch, atop known-good `7226797`) on
2026-07-29. Verified by: CC (self-verified per-row above, incl. a full structural cross-check against
the live corpus); CDC review found the iteration-1 regression below.

**Iteration 1 close** (F-16…F-21): CDC verification of the base close reproduced the live store and
found 4 nodes (#22–25) carrying absolute `source.paths` — F-10's "no model drift" claim falsified.
Fixed on `release/1.0.x` (seam fix `ff68186`, corrective-mechanism `3eddf38`) and fired live as one
revertible commit `2fc25f5` on `odm`, atop `355404a`, on 2026-07-29. Verified by: CC (self-verified
per-row above, incl. before/after `odm check` reproduce-the-bug evidence and a byte-level `git diff` of
all 4 rewritten nodes); CDC re-verification pending.

Rows: 21 (15 base + 6 iteration-1). Done: 19. Deferred: 2 (F-20 ODD-0025 wording, flagged for the
operator/s12; F-21 stale `ROLLUP.md`, flagged as housekeeping). No-op: 0.

Undo (base): `git reset --hard 7226797`. Undo (iteration 1 only, keeping the base mint):
`git reset --hard 355404a`. On close, bubble up to `../arc-plan.md`: MF-1/MF-6 done (enforced live,
now durably so — the invariant that made this regression possible is itself gated); MF-3 done (full
criterion, less 2 disclosed drifted nodes deferred to s12); **s11 (synthesis + L-8b) next**;
living-doc-drift reconcile → s12 (now covering the 2 drifted design nodes from F-6, plus F-20's ODD-0025
wording touch-up); s10 flips to CDC-verified PASS only once CDC reproduces 0 absolute `source.paths` +
the enforced invariant.
